React = require 'react'
{ useState, useEffect } = require 'react'
{ $ } = require('mambascript/decision')(React)



PrimaryHeader = ->
  $ 'header',{ className: 'app-banner-primary'}, =>
    $ 'div', { className: 'cover' }
    $ 'img', { className: 'logo', src: '/img/logo.png'}
module.exports = PrimaryHeader
